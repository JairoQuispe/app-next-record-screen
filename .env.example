# Cloudflare Worker URL for Whisper-large-v3-turbo transcription
# Deploy the worker from src-workers/whisper-transcription/ and set the URL here
VITE_WHISPER_WORKER_URL=https://whisper-transcription.<your-subdomain>.workers.dev

# ── Hybrid Architecture (Tauri + Cloudflare) ─────────────────────
# Base URL for the Cloudflare Workers API (leave empty for same-origin in production)
VITE_API_BASE_URL=http://localhost:8787

# Force runtime detection (optional: "cloudflare" to test cloud mode locally)
# VITE_RUNTIME=cloudflare

# Enable cloud features (R2 storage, cloud transcription, etc.)
VITE_CLOUD_ENABLED=false
